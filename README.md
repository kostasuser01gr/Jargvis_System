
  # Jarvis System

  This is a code bundle for Jarvis System. The original project is available at https://www.figma.com/design/aweulH8gv0mv5XPcooBnbF/Jarvis-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  